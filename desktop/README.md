# Browser UI

The browser UI served by `sopsdeck drive` lives in `src/`. The supported user install is the npm app described in the repo-root README.

```bash
./scripts/dev
```

That builds `./sopsdeck` and launches the browser app with `SOPSDECK_BIN` set to it. Full instructions are in the repo-root README.
